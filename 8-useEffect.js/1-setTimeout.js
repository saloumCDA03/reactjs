/**
Créez un nouveau composant React appelé TimerComponent.
Dans ce composant, initialisez un état  à 0
utilisez setTimeout() pour incrémenter l'etat de 1 ( setSeconde(seconde+1) )

*/

/**
 import React, { useState } from 'react';

function TimerComponent() {
  const [seconde, setSeconde] = useState(0);

  setTimeout(() => {
    setSeconde(seconde + 1);
  }, 1000);

  return <div>Secondes écoulées : {seconde}</div>;
}

export default TimerComponent;

 */
